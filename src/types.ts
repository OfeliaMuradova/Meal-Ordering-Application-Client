export default interface Meal{
  qty: number;
  code: string;
  type: string;
  description: string;
  singlePrice: number;
}

export default interface Order{
  
}

export enum Workdays{
  Monday,
  Tuesday,
  Wednesday,
  Thursday,
  Friday
}
